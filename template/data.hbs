{
    "blog": [
        {{#each data as | value |}}
            {
                {{#each value}}
                    "{{@key}}": "{{this}}"{{#unless @last}},{{/unless}}
                {{/each}}
                }{{#unless @last}},{{/unless}}
        {{/each}}
    ]
}